# ADR-014 — Reconstruction Visual & Epistemic Surface (Iteration 3)

**Status:** Iteration 3 — SHIPPED (verified). Future phases gated behind user green-light.
**Date:** 2026-08-25
**Engine integrity:** ZERO engine/API/save/narration/investigation-logic changes. All additions are additive UI + one additive payload field.

---

## 1. Mandate

Iteration 3 makes the reconstruction *visible* and the epistemic surface *legible*, without touching the frozen spine:

- **Phase A — `challenge` affordance (engine-existing):** the ADR-011 free-chat already supports the `challenge` action ("interrogate a reconstruction/testimony line"). The literary surface exposes it as a collapsed, never-blocking command hint. Shipped in Iteration 2 UI.
- **Phase B — R3F reconstruction visual:** render the existing deterministic `buildReconstructionState` adapter as a Three.js fragment field. Read-only; carries the **Two-Chris gap** through visual grammar. No new epistemic logic.
- **Phase C — divergence alerts:** promote existing `visibleContradictions` into actionable, epistemic-framed alerts when a stronger (canonical) claim is in tension with a weaker (mythos) reconstruction claim. Additive field on the board payload; surfaced in the Consistency Board.

## 2. Visual Grammar (Phase B)

Deterministic boundary: `WorldState -> buildReconstructionState -> <ReconstructionScene>`. Same inputs → same scene (no `Math.random` in placement; drift uses time only).

| Source / status | Treatment | Meaning |
|---|---|---|
| canonical / inferred / observation | solid, cool white, settled | "real Chris bone" |
| reddit / author / compiled_event | solid, pale cyan | anchored to source |
| conversation (mythos) | stitched (wireframe, jitter, flicker), amber | woven from the reconstruction's delusions |
| belief / hypothesis / rumor (unsourced) | dim, drifting | unanchored |

Legend text (verbatim in UI): `■ real Chris bone (canonical)` · `□ stitched from mythos` · `· unanchored / drifting`.

The scene is **behind a toggle** (`.scene-toggle`) for performance — it mounts a WebGL canvas only when opened.

## 3. Divergence Alerts (Phase C)

- Computed in `buildInvestigationPayload` from `visibleContradictions`. For each, the contradicting fact node is classified by `kind` + `claimedBy`:
  - `strongerSource` = `canonical` when the fact/evidence node is canonical-tier; otherwise its `claimedBy`.
  - `weakerSource` = `mythos` when the contradicting node is `belief`/`claim`; otherwise its `kind`.
- Surfaced in the Consistency Board as a `DIVERGENCE ALERTS` section, rendered with `strongerSource ⊣ weakerSource · re-read source or challenge the claim` — never asserting world-truth, always pointing back to the source.
- Added to the single `InvestigationPayload` shape; unioned in `aggregateInvestigation`.

## 4. Verification

- `tsc -p tsconfig.json --noEmit` → 0 errors.
- `vitest run` → **177 passed** (incl. new `tests/challenge.test.ts` — deterministic `doChallenge` loop: ledger records `challenge.<factId>`, fail-closed concede when no source).
- `npm run build` → clean (static prerender).
- `playwright test` → **5 passed**, including: R3F `.recon-scene` canvas mounts; `.recon-detail` shows epistemic source label; click-to-challenge runs the engine loop (`> you challenge:` line recorded to log).

## 5. Checkpoints resolved (green-lit, shipped this pass)

1. **Epistemic interaction depth (§5.1)** — `challenge` is now a first-class *click-driven* loop. The engine already dispatched `challenge` → `doChallenge` (deterministic, hash-seeded, fail-closed) in all four episodes; the missing link was the surface. `GameClient.challengeClaim(factId)` builds a typed `challenge` action (`targetId = factId`) and runs it through the **existing** `processTurnWithAction` pipeline (no LLM, no engine change). Wired to three surfaces:
   - Consistency Board **DIVERGENCE ALERTS** → "challenge the claim" button.
   - Evidence provenance block → "challenge this claim" button.
   - R3F reconstruction detail panel → "challenge this fragment" button (`onChallengeClaim` prop).
   Each click records `challenge.<factId>` to the ledger and refreshes the Board against the new state.
2. **Investigation redesign (§5.2)** — the Board now *drives* the player two ways: (a) open leads each expose an "investigate" button (engine challenge loop), and (b) a proactive **SUGGESTED NEXT** nudge surfaces the top open lead (highest graph centrality) as a one-tap action. `examine the phone` (ep1) surfaces the genuinely-unresolved `ep1.mother.knows` (status `unknown`, per source) as a known lead so the affordances are reachable in normal play. All content/UI only — no engine-logic change. The `suggestedNext` field is additive on `InvestigationPayload` (both the `lib/core` and `components` copies were updated — the long-standing duplicate-interface smell, flagged in §1, now bites; a unification ADR is still owed).

## 7. Checkpoints fully resolved

All three ADR-014 checkpoints are shipped:
- §5.1 challenge loop (click-driven, engine-safe) ✓
- §5.2 Board drives player (open-lead investigate + suggested-next auto-prompt) ✓
- R3F scene interactivity (fragment → detail + challenge) ✓

## 6. Checkpoints still open (require explicit green-light)

3. **R3F scene interactivity** — superseded: fragment click → detail panel with source + challenge is shipped (Phase B/C complete).

## 7. Frozen constraints honored

- Engine, API contracts, save format, narration, investigation logic, `ReconstructionState` adapter: **unchanged** by §5.1/§5.2. `challengeClaim` is UI orchestration over the existing deterministic `doChallenge`.
- Fail-closed TTS: untouched.
- Epistemic honesty: alerts report divergence; the engine never asserts reconstruction claims as world-truth (concede branch when no source).
- Two `InvestigationPayload` defs: **unified** — `components/investigation-payload.ts` now re-exports the canonical interface from `lib/core/investigation.ts`; `timelines?: string[]` lives on the single definition (carried by the cross-timeline aggregate view).
